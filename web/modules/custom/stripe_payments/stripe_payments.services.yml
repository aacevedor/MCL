services:
  logger.channel.stripe_payments:
    parent: logger.channel_base
    arguments: ['stripe_payments']
  stripe_payments.create:
    class: Drupal\stripe_payments\StripePaymentsCreate
    arguments: []
